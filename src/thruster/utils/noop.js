define(function(){
	
	/**
	 * A null-operation function. Can be used where a callback function needs to be passed to a
	 * function, but the result is not needed.
	 * @memberof thruster.utils
	 * @static
	 */
	var noop = function(){};
	
	return noop;
	
});