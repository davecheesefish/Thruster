define(function(){
	
	/**
	 * Class representing a point in 2-dimensional Euclidean space.
	 * @constructor
	 * @param {Number} x
	 * @param {Number} y
	 */
	var Point2d = function(x, y){
		this.x = x;
		this.y = y;
	};
	
	return Point2d;
	
});